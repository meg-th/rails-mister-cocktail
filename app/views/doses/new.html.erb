<div class="container">
  <%= simple_form_for [ @cocktail, @dose ] do |f| %>
    <%= f.input :description %>
    <%= f.association :ingredient %>
    <div class="btn btn-primary"> <%= f.button :submit %> </div>
  <% end %>
</div>